syntax = "proto3";

// protobuf的package，根据业务来定，一个文件夹下的package得是一样的
package person;

// 和package保持一致
option go_package = "person";

// 导入需要使用的文件，package/文件名
import "person/person_info.proto";


// rpc服务
service PersonService {
  rpc GetPersonInfo(GetPersonReq) returns (GetPersonRsp) {}
}

